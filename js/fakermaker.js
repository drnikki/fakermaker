/**
 * @file
 * FakerMaker module behaviors.
 */

(function ($, Drupal) {

  'use strict';

  /**
   * Add some explaination of existance here
   *
   * @type {Drupal~behavior}
   *
   * @prop {Drupal~behaviorAttach} attach
   *   Attaches the behavior for the some fakermaker fun.
   */
/*
  Drupal.behaviors.fakerMakerThings = {
    attach: function (context, settings) {
      if (settings.SOMESETTING) {
      }
    }
  };
*/

}(jQuery, Drupal));
